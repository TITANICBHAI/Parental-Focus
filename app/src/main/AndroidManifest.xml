<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ══════════════════════════════════════════════════
         NORMAL PERMISSIONS (granted at install time)
    ══════════════════════════════════════════════════ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.EXPAND_STATUS_BAR" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- ══════════════════════════════════════════════════
         RUNTIME PERMISSIONS (user must grant)
    ══════════════════════════════════════════════════ -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- ══════════════════════════════════════════════════
         SPECIAL PERMISSIONS (Settings → Special app access)
    ══════════════════════════════════════════════════ -->

    <!-- Accessibility Service – required for foreground app detection & HOME action -->
    <!-- Granted in: Settings → Accessibility → Parental Focus -->

    <!-- Usage Stats – query which app is in foreground on older APIs -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Draw Over Other Apps – BlockOverlayActivity needs this on some devices -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Query all installed packages (needed to list apps to block) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Brightness write (auto-dim when focus mode active) -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <!-- ══════════════════════════════════════════════════
         HARDWARE FEATURES
    ══════════════════════════════════════════════════ -->
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".ParentalFocusApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParentalFocus"
        android:requestLegacyExternalStorage="false"
        tools:targetApi="31">

        <!-- ── Main entry Activity ── -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ── Face Enrollment Activity ── -->
        <activity
            android:name=".ui.FaceEnrollmentActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- ── Block Overlay Activity ──
             Full-screen overlay shown when a blocked app is detected.
             • excludeFromRecents — never appears in app switcher
             • showWhenLocked / turnScreenOn — works over lock screen
             • noHistory — removed from back stack on finish
             • singleTask — prevents duplicate stacking -->
        <activity
            android:name=".overlay.BlockOverlayActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:noHistory="false"
            android:launchMode="singleTask"
            android:theme="@style/Theme.ParentalFocus.Fullscreen" />

        <!-- ── Blocker Foreground Service ──
             Stays alive to keep AccessibilityService process running. -->
        <service
            android:name=".service.BlockerForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="parental_control" />
        </service>

        <!-- ── Accessibility Service ──
             Core blocking engine. Intercepts window events and fires HOME. -->
        <service
            android:name=".service.AppBlockerAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- ── Boot Receiver ── -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- ── Schedule Check Worker (AlarmManager receiver) ── -->
        <receiver
            android:name=".service.ScheduleAlarmReceiver"
            android:exported="false" />

    </application>

</manifest>
